#include<iostream>
using namespace std;

procedure PLACE(k)
global X(l: k ); integer i, k
for i - 1 to k do
if X(i) = X(k)
or ABS(X(i) - X(k)) = ABS(i - k)
then 
return(false)
endif
repeat
return( true)
end PLACE

bool place(int k)
{
    
}

int main()
{


    return 0;
}